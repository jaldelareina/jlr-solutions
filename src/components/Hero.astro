<section class="hero">
  <div class="hero-content">
    <p class="eyebrow">Automatización · IA · Integración</p>

    <h1>Automatización e Inteligencia Artificial para impulsar tu negocio</h1>

<p class="hero-text">
  Automatizamos procesos, conectamos sistemas e incorporamos IA donde realmente aporta valor:
  menos tareas repetitivas, menos errores y más tiempo para lo importante.
</p>

    <div class="buttons">
      <a class="btn primary" href="/servicios">Ver servicios</a>
      <a class="btn secondary" href="/contacto">Contacto</a>
    </div>
  </div>
</section>

<style>
.hero {
  min-height: calc(100vh - 96px);
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  color: white;
  position: relative;
  overflow: hidden;
  padding: 80px 20px;

  background: linear-gradient(-45deg, #0b0f19, #0a1d33, #132a4f, #0d1b2e);
  background-size: 400% 400%;
  animation: gradientAnimation 18s ease infinite;
}

@keyframes gradientAnimation {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.hero-content {
  max-width: 940px;
  width: 100%;
  animation: fadeInUp 1.2s ease forwards;
  opacity: 0;
}

.eyebrow {
  display: inline-block;
  margin: 0 0 18px;
  padding: 8px 14px;
  border: 1px solid rgba(88, 166, 255, 0.35);
  border-radius: 999px;
  color: #93c5fd;
  background: rgba(88, 166, 255, 0.08);
  font-size: 0.95rem;
}

h1 {
  margin: 0;
  font-size: clamp(2.4rem, 6vw, 4.6rem);
  font-weight: 750;
  line-height: 1.05;
  letter-spacing: -1.2px;
}

.hero-text {
  max-width: 760px;
  margin: 1.3rem auto 0;
  font-size: clamp(1.05rem, 2.4vw, 1.35rem);
  line-height: 1.65;
  opacity: 0.9;
}

.buttons {
  margin-top: 2.2rem;
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.btn {
  min-width: 160px;
  padding: 0.95rem 1.8rem;
  border-radius: 12px;
  font-weight: 700;
  text-decoration: none;
  transition: 0.2s ease-in-out;
}

.btn.primary {
  background: #58a6ff;
  color: #000;
}

.btn.primary:hover {
  background: #79c0ff;
}

.btn.secondary {
  color: white;
  border: 2px solid #58a6ff;
}

.btn.secondary:hover {
  background: #58a6ff;
  color: #000;
}

@keyframes fadeInUp {
  0% { opacity: 0; transform: translateY(25px); }
  100% { opacity: 1; transform: translateY(0); }
}

@media (max-width: 768px) {
  .hero {
    min-height: auto;
    padding: 72px 18px 56px;
    align-items: flex-start;
  }

  .eyebrow {
    font-size: 0.85rem;
    margin-bottom: 16px;
  }

  h1 {
    font-size: clamp(2rem, 10vw, 3rem);
    line-height: 1.08;
    letter-spacing: -0.8px;
  }

  .hero-text {
    font-size: 1rem;
    line-height: 1.55;
    margin-top: 1rem;
  }

  .buttons {
    flex-direction: column;
    gap: 0.85rem;
    margin-top: 1.8rem;
  }

  .btn {
    width: 100%;
    max-width: 320px;
    margin: 0 auto;
  }
}

@media (max-width: 380px) {
  .hero {
    padding: 56px 14px 44px;
  }

  h1 {
    font-size: 1.9rem;
  }

  .hero-text {
    font-size: 0.96rem;
  }
}
</style>
