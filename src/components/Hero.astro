<section class="hero">
  <div class="hero-content">
    <p class="eyebrow">Automatización · IA · Integración</p>

<h1>Automatiza tareas, conecta sistemas y gana tiempo para tu negocio</h1>

<p class="hero-text">
  Creamos soluciones con automatización, integración e inteligencia artificial para reducir trabajo manual,
  evitar errores y mejorar tus procesos.
</p>

    <div class="buttons">
      <a class="btn primary" href="/contacto">Contactar</a>
      <a class="btn secondary" href="/servicios">Ver servicios</a>
    </div>
  </div>
</section>

<style>
.hero {
  min-height: calc(100vh - 96px);
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  color: white;
  position: relative;
  overflow: hidden;
  padding: 6rem 1.5rem 4rem;
  background:
    radial-gradient(circle at top left, rgba(88, 166, 255, 0.18), transparent 34%),
    radial-gradient(circle at bottom right, rgba(124, 247, 255, 0.12), transparent 32%),
    #080d16;
}

.hero-content {
  max-width: 1050px;
  width: 100%;
  animation: fadeInUp 1.1s ease forwards;
  opacity: 0;
}

.eyebrow {
  display: inline-block;
  margin: 0 0 18px;
  padding: 8px 14px;
  border: 1px solid rgba(88, 166, 255, 0.35);
  border-radius: 999px;
  color: #93c5fd;
  background: rgba(88, 166, 255, 0.08);
  font-size: 0.95rem;
}

h1 {
  margin: 0;
  font-size: clamp(2.6rem, 7vw, 5rem);
  font-weight: 800;
  line-height: 1.04;
  letter-spacing: -1.4px;
}

.hero-text {
  max-width: 820px;
  margin: 1.4rem auto 0;
  color: rgba(228, 232, 239, 0.86);
  font-size: clamp(1.08rem, 2.4vw, 1.35rem);
  line-height: 1.65;
}

.buttons {
  margin-top: 2.2rem;
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.btn {
  min-width: 170px;
  padding: 0.95rem 1.7rem;
  border-radius: 14px;
  font-weight: 800;
  text-decoration: none;
  transition: 0.2s ease;
}

.btn.primary {
  background: #58a6ff;
  color: #000;
}

.btn.primary:hover {
  background: #7cf7ff;
  color: #000;
}

.btn.secondary {
  color: white;
  border: 2px solid rgba(88, 166, 255, 0.75);
}

.btn.secondary:hover {
  background: rgba(88, 166, 255, 0.16);
  color: white;
}

@keyframes fadeInUp {
  0% { opacity: 0; transform: translateY(25px); }
  100% { opacity: 1; transform: translateY(0); }
}

@media (max-width: 768px) {
  .hero {
    min-height: auto;
    padding: 4.5rem 1rem 3.2rem;
  }

  h1 {
    font-size: clamp(2.15rem, 10vw, 3.2rem);
    line-height: 1.08;
  }

  .hero-text {
    font-size: 1rem;
    line-height: 1.55;
  }

  .buttons {
    flex-direction: column;
  }

  .btn {
    width: 100%;
    max-width: 340px;
    margin: 0 auto;
  }
}
</style>
